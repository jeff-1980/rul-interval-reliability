\documentclass[journal,onecolumn]{IEEEtran}
\usepackage{amsmath,booktabs,multirow,longtable,array}
\newcommand{\PICP}{\mathrm{PICP}}\newcommand{\IS}{\mathrm{IS}}\newcommand{\WIS}{\mathrm{WIS}}\newcommand{\foob}{f_{\mathrm{oob}}}
\input{tables/numbers.tex}
\makeatletter\newcommand{\tabin}[1]{\@@input #1 }\makeatother
\begin{document}\sloppy
\title{Supplementary Material for ``Reliability Failure of Remaining-Useful-Life Prediction Intervals under Sensor Degradation''}
\author{Yi Wang}
\maketitle
\noindent Every table below is generated by script from the named source file in the released repository (\texttt{jeff-1980/\allowbreak rul-interval-reliability}, tag \texttt{v1.4-submission}); no value is transcribed by hand. All perturbations are seeded from SHA-256 digests; with \texttt{PYTHONHASHSEED=0} and deterministic cuDNN, two independent runs on the same machine reproduced \NReproBit{} result files bit-for-bit and the remainder in every field except a wall-clock timing field; the files are listed with their hashes in \texttt{REPRODUCE.md}.

\section*{S0. Provenance of every table}
Each main-text and appendix table, with the generating script in \texttt{code/} of the released repository, the label version (``clipped'' is the 125-cycle target clip), the evaluation unit, whether engines are equally weighted, how seeds and perturbation realisations are aggregated, and the data over which perturbation scales are referenced.
\begin{center}\tiny
\begin{longtable}{p{2.2cm}p{4.6cm}p{1.5cm}p{1.5cm}p{0.9cm}p{2.4cm}p{1.2cm}}
\toprule
Table & Script / result file & Labels & Unit & Engine-weighted & Aggregation & Noise range \\
\midrule
\endhead
\tabin{tables/supp_s0.tex}\bottomrule
\end{longtable}
\end{center}

\section*{S1. Four-way attribution at the interpolated ten-point-loss point}
Source: \texttt{results/attribution/attribution\_lstm\_exact\_interp.json} and \texttt{results/attribution/attribution\_transformer\_exact\_interp.json}. $C_{ij}$ is coverage with mean output from condition $i$ and scale output from condition $j$ ($0$ clean, $1$ perturbed). Order A varies the mean first; order B varies the scale first. Interaction $=(C_{11}-C_{10})-(C_{01}-C_{00})$, computed from the four coverages; it is positive in all sixteen cells and reaches $+0.043$.

\begin{table*}[!h]\centering\footnotesize
\caption{Four-way attribution, both backbones, at the interpolated point ($\foob$ in \%).}
\begin{tabular}{lllccccccccccc}
\toprule
Backbone & Data & Mech. & $\foob$ & $C_{00}$ & $C_{10}$ & $C_{01}$ & $C_{11}$ & $\mu$ (A) & $\sigma$ (A) & $\mu$ (B) & $\sigma$ (B) & Int. & Both \\
\midrule
\tabin{tables/supp_s1.tex}\bottomrule
\end{tabular}
\end{table*}

\noindent Cross-seed standard deviations of $C_{10}$ and $C_{11}$ at the grid point nearest the crossover (\texttt{results/attribution/attribution\_raw\_per\_seed\_nearest\_grid.json}) lie between 0.011 and 0.081; the percentage contributions in the main text are computed from seed-pooled predictions (aggregate-then-difference), not as a mean of per-seed ratios.


\noindent\textbf{Paired bootstrap of mean dominance.} For each cell, $|\Delta_\mu|-|\Delta_\sigma|$ was computed per seed at the same interpolated ten-point-loss point as the table above (per-seed $C_{ij}$ interpolated between the same bracketing levels), separately for each decomposition order, and bootstrapped over the five seeds (2000 resamples). The intervals reflect variability across the five training-and-partition seeds only; they are conditional on the fixed test set, the seeded perturbation realisations and the chosen interpolated crossover, and do not propagate those sources of uncertainty. Source: \texttt{results/attribution/attribution\_bootstrap\_by\_order\_exact\_interp.json}.
\begin{center}\small\begin{tabular}{lllcccc}\toprule
Backbone & Data & Mech. & Mean first: mean [95\% CI] & $>0$ & Scale first: mean [95\% CI] & $>0$ \\ \midrule
\tabin{tables/supp_boot.tex}\bottomrule\end{tabular}\end{center}

\section*{S2. Terminal-instant decision stress test, full results}
Source: \texttt{results/decision/maintenance\_decision\_one\_sided.json}. One-sided 95\% lower bound; unrec.\ = unrecognised maintenance need (overall / conditional on true RUL $\le L$); prem.\ = premature trigger rate; RUL@t = true RUL at premature trigger under the 125-cycle label clip (unclipped values for the main-text rows are in Table~VII of the paper); cost = $c_{\mathrm{late}} r_{\mathrm{late}} + c_{\mathrm{early}} r_{\mathrm{early}}$ with $c_{\mathrm{early}}=1$; rank at ratios 5/20/100.

\begin{center}\footnotesize
\begin{longtable}{lllllcclll}
\toprule
Backbone & Data & Cond. & $L$ & Mech. & unrec. (ovr / cond.) & prem. & RUL@t & cost 5/20/100 & rank \\
\midrule
\endhead
\tabin{tables/supp_s2.tex}\bottomrule
\end{longtable}
\end{center}

\section*{S2b. Rank reversal across cost ratios}
Reversal: the ranking of the five mechanisms differs between at least two of the cost ratios; top change: the top-ranked mechanism differs. Totals: 36 of 96 reversals, 20 top changes.
\begin{center}\footnotesize
\begin{longtable}{llllcc}
\toprule
Backbone & Data & Cond. & $L$ & Reversal & Top change \\
\midrule
\endhead
\tabin{tables/supp_s2b.tex}\bottomrule
\end{longtable}
\end{center}

\section*{S2c. Per-engine bootstrap of top-ranked mechanism against the others}
For each (condition, $L$, ratio) the cost difference between the top-ranked mechanism and each of the other four was bootstrapped over engines (2000 resamples). Number of the 144 comparisons per backbone--dataset whose 95\% interval excludes zero:
\begin{center}\begin{tabular}{llc}\toprule Backbone & Data & Excluding zero \\ \midrule
\tabin{tables/supp_s2c.tex}\bottomrule\end{tabular}\end{center}
The full 1152 intervals are in the source file.

\section*{S3. Single-level weighted interval score}
Sources: \texttt{results/clean/interval\_score\_wis\_clean.json} (heteroscedastic, ensemble, CP-norm) and \texttt{results/clean/fair\_calibration\_main\_table.json} (fixed variance and MC Dropout, calibration-set scale). With one interval level ($\alpha=0.10$, $K=1$) the score used is
$\WIS=\frac{1}{K+1/2}\left[\tfrac12|y-\hat\mu|+\tfrac{\alpha}{2}\,\IS_\alpha\right]$, with $\hat\mu$ in place of the median. Means over the terminal unit, clean inputs, five seeds (ensemble: single $M=5$).
\begin{center}\small\begin{tabular}{llccccc}\toprule
Backbone & Data & Heterosc. & Fixed var. & MC Drop. & Ensemble & CP-norm \\ \midrule
\tabin{tables/supp_s3.tex}\bottomrule\end{tabular}\end{center}

\section*{S4. Latency distributions}
Source: \texttt{results/latency/lstm\_latency.json}. Per-sample latency in microseconds, median (IQR) over eleven repetitions, one NVIDIA RTX A5000 Laptop GPU; no repetition excluded.
\noindent\textit{CUDA-event timing}
\begin{center}\scriptsize
\begin{longtable}{lllccccc}
\toprule
Data & Backbone & Batch & Heterosc. & Fixed var. & CP-norm & MC Drop. ($T$=50) & Ensemble ($M$=5) \\
\midrule
\endhead
\tabin{tables/supp_s4_cuda.tex}\bottomrule
\end{longtable}
\end{center}
\noindent\textit{Wall-clock timing}
\begin{center}\scriptsize
\begin{longtable}{lllccccc}
\toprule
Data & Backbone & Batch & Heterosc. & Fixed var. & CP-norm & MC Drop. ($T$=50) & Ensemble ($M$=5) \\
\midrule
\endhead
\tabin{tables/supp_s4_wall.tex}\bottomrule
\end{longtable}
\end{center}

\end{document}
